{
  "license": "ISC",
  "scripts": {
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
    "_start:validator": "lsof -i :9900 -t | xargs -r kill -9; solana-test-validator --reset --faucet-port 9901 --quiet",
    "_stop:validator": "pkill -f solana-test-validator || true",
    "_build": "anchor build",
    "_deploy": "solana program show YBuXX3zr2he3tiZgzbethftfi65S4oThG6u46QniRNG >/dev/null 2>&1 || anchor deploy",
    "_mocha": "ts-mocha -p tsconfig.json -t 1000000 tests/**/*.ts",
    "test": "ANCHOR_PROVIDER_URL=http://127.0.0.1:8899 ANCHOR_WALLET=$HOME/.config/solana/id.json npm run _start:validator & sleep 3 && npm run _build && npm run _deploy && npm run _mocha && npm run _stop:validator",
    "test:local": "ANCHOR_PROVIDER_URL=http://127.0.0.1:8899 ANCHOR_WALLET=$HOME/.config/solana/id.json npm run _start:validator & sleep 3 && npm run _build && npm run _deploy && npm run _mocha && npm run _stop:validator"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/spl-token": "^0.4.14"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "prettier": "^2.6.2",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
